## Loading and preprocessing the data  

## Step 1 - Download the Zip file to data folder - check if data folder exists
```{r echo = TRUE}
if (!file.exists("data")) {
  dir.create("data")
} else {
  print("Folder data exists!")
}

fileURL <- c("http://d396qusza40orc.cloudfront.net/repdata/data/activity.zip")
download.file(fileURL, destfile = ".\\data\\dataset.zip")

zipFile <- c(".\\data\\dataset.zip")
unzip(zipFile, exdir = ".\\data")
```

## What is mean total number of steps taken per day?
```{r echo = TRUE}
```

## What is the average daily activity pattern?
```{r echo = TRUE}
```

## Imputing missing values
```{r echo = TRUE}
ActFile <- c(".\\data\\activity.csv")
data1 <- read.csv(ActFile, colClasses = "character", na.strings = "Not Available")
```

## Are there differences in activity patterns between weekdays and weekends?
```{r echo = TRUE}
```